
    /** The Type representing the Metadata stored with every Instance
     *  to track statistics, security, and so on (if used). */
    public static final Type<Metadata> METADATA =
        new Type<Metadata> ( TypingEnvironment.NONE,
                             Metadata.class,
                             new Tag [ 0 ],
                             "metadata",
                             Type.NONE,
                             Metadata.NONE );
