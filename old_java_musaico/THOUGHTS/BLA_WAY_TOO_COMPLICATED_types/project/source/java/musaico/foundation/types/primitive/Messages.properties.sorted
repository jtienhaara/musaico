Invalid\ primitive:\ "					%]No\ caster\ from\ existing\ "					%]primitive\ type\ "					\ +\ primitives\ [\ p\ ]					%]\ to\ new\ type\ "					\ +\ type = Invalid primitive: "					%]No caster from existing "					%]primitive type "					 + primitives [ p ]					%] to new type "					 + type
Invalid\ primitive:\ "					%]No\ caster\ from\ new\ type\ "					\ +\ type					%]\ to\ existing\ "					%]primitive\ type\ "					\ +\ primitives\ [\ p\ ] = Invalid primitive: "					%]No caster from new type "					 + type					%] to existing "					%]primitive type "					 + primitives [ p ]
Only\ Serializable\ classes\ can\ "				%]be\ used\ as\ primitives.\ "				\ +\ raw_class%]\ (represented\ "				%]by\ type\ "				\ +\ type				%])\ is\ not\ serializable\ so\ "				%]cannot\ be\ added\ to\ type\ system\ "				\ +\ this = Only Serializable classes can "				%]be used as primitives. "				 + raw_class%] (represented "				%]by type "				 + type				%]) is not serializable so "				%]cannot be added to type system "				 + this
